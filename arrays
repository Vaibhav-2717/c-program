include <bits/stdc++.h>

using namespace std;

vector<string> split_string(string);



int main()
 {
    int n,ar[10005];
    cin >> n;
    for(int i = 0; i < n; i++)cin >> ar[i];
    for(int i = n-1; i >=0; i--)cout << ar[i] << " ";
    cout << endl;
    return 0;
}
   
